class S3BackendConfig {
    bucket: String
    region: String
    key: String
    use_lockfile: Boolean = true
    profile: String?
}

class LocalBackendConfig {
    path: String
}

typealias BackendConfig = S3BackendConfig | LocalBackendConfig